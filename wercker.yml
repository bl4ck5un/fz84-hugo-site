box: wercker/default
build:
  steps:
    - arjen/hugo-build:
        version: 0.13
        theme: redlounge
        flags: --disableSitemap=true

deploy:
  steps:
    - gh-pages:
        token: $GIT_TOKEN
        domain: bl4ck5un.github.io
        basedir: public
